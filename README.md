# build
docker-compose up -d

docker-compose start
docker-compose exec react /bin/sh
docker-compose stop



# create project
npx create-react-app (name) --typescript

# run react
cd (name)
yarn start


# blowser
localhost:3000
